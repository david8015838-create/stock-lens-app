{
  "name": "stock-lens",
  "version": "1.0.0",
  "private": true,
  "description": "Real-time stock portfolio tracker with Yahoo Finance live prices",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static site — no build step required'",
    "start": "vercel dev"
  },
  "dependencies": {},
  "engines": {
    "node": "22.x"
  }
}
